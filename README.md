# BLDC-Drone
A small drone built using brushless dc motors. Not sure if I'm going to move forward with the Arduino or the STM32 version. Will probably make both, so won't merge the branches into main.
